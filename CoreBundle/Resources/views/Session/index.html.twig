{% extends 'PivotXCoreBundle::base.html.twig' %}

{% block crud %}

<h1>Session list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Ip</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('session_show', { 'id': entity.id }) }}" class="btn">{{ entity.id }}</a></td>
            <td>{{ entity.ip }}</td>
            <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif%}</td>
            <td>
                <ul class="record_actions">
                    <li>
                        <a href="{{ path('session_show', { 'id': entity.id }) }}" class="btn">show</a>
                    </li>
                    <li>
                        <a href="{{ path('session_edit', { 'id': entity.id }) }}" class="btn">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('session_new') }}" class="btn">
            Create a new entry
        </a>
    </li>
</ul>

{% endblock %}
