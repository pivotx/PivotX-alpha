{% extends 'PivotXCoreBundle::base.html.twig' %}

{% block crud %}

<h1>Taxonomytype creation</h1>

<form action="{{ path('taxonomytype_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('taxonomytype') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}
