{% extends 'PivotXCoreBundle::base.html.twig' %}

{% block crud %}

<h1>Userpermission list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('userpermission_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>
                <ul class="record_actions">
                    <li>
                        <a href="{{ path('userpermission_show', { 'id': entity.id }) }}" class="btn">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userpermission_edit', { 'id': entity.id }) }}" class="btn">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('userpermission_new') }}" class="btn">
            Create a new entry
        </a>
    </li>
</ul>

{% endblock %}
