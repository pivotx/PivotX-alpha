{% extends 'PivotXCoreBundle::base.html.twig' %}


{% block crud %}
    
<h1>Taxonomyrelation list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Sortingorder</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('taxonomyrelation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.sortingOrder }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('taxonomyrelation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('taxonomyrelation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('taxonomyrelation_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

{% endblock %}
