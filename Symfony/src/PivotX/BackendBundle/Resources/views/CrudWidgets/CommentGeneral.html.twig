        <ul class="nav nav-list">
            <li class="nav-header">General information</li>
            <li>
                <p>
                    <strong>{{ item.title }}</strong>
                {% if items.length == 0 %}
                    has no comments.
                {% elseif items.length == 1 %}
                    has {{ items.length }} comment.
                {% else %}
                    has {{ items.length }} comments.
                {% endif %}
                </p>

                <p>
                    Go <a href="{{ ref(['_table/',crud.entityref]|join) }}">back</a> to {{ translate(['entity.name.plural.',crud.entity]|join) }}.
                </p>
            </li>
        </ul>
